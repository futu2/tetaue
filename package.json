{
  "name": "tetaue",
  "version": "0.1.0",
  "description": "tetaue — a pure functional SQL query language, built with Langium on bun/TypeScript. Inspired by futu2/teta and mikitori/moria.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "tetaue": "./dist/cli.mjs"
  },
  "files": [
    "dist",
    "bin",
    "src",
    "examples",
    "prelude.tetaue",
    "README.md"
  ],
  "scripts": {
    "langium:generate": "langium generate",
    "build": "bun run langium:generate",
    "build:cli": "bun build src/cli.ts --target=node --outfile=dist/cli.mjs --banner '#!/usr/bin/env node'",
    "prepack": "bun run build:cli",
    "check": "tsc --noEmit",
    "test": "bun test",
    "render": "bun run src/cli.ts render",
    "check:examples": "bun run src/cli.ts check examples/adults.tetaue && bun run src/cli.ts check examples/orders.tetaue && bun run src/cli.ts check examples/strings.tetaue && bun run src/cli.ts check examples/report.tetaue && bun run src/cli.ts check examples/joins.tetaue && bun run src/cli.ts check examples/namespaced.tetaue && bun run src/cli.ts check examples/selective.tetaue && bun run src/cli.ts check examples/case.tetaue && bun run src/cli.ts check examples/lpbirthday.tetaue && bun run src/cli.ts check examples/tables.tetaue && bun run src/cli.ts check examples/predicates.tetaue && bun run src/cli.ts check examples/lib-project/main.tetaue",
    "validate:sqlite": "bun run scripts/validate-sqlite.ts",
    "build:server": "bun build src/language-server.ts --target=node --format=esm --outfile extension/server/server.mjs",
    "build:client": "cd extension && bun run compile",
    "build:extension": "bun run build:server && bun run build:client",
    "package:extension": "bun run build:extension && cd extension && bun run package",
    "build:standalone": "bun run scripts/build-standalone.ts --host",
    "build:standalone:all": "bun run scripts/build-standalone.ts",
    "build:standalone:portable": "bun run scripts/build-standalone.ts --official-runtime",
    "build:standalone:linux": "bun run scripts/build-standalone.ts bun-linux-x64-musl",
    "build:standalone:linux-arm": "bun run scripts/build-standalone.ts bun-linux-arm64-musl",
    "build:standalone:macos": "bun run scripts/build-standalone.ts bun-darwin-x64",
    "build:standalone:macos-arm": "bun run scripts/build-standalone.ts bun-darwin-arm64",
    "build:standalone:windows": "bun run scripts/build-standalone.ts bun-windows-x64"
  },
  "dependencies": {
    "langium": "~4.3.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^24.0.0",
    "langium-cli": "~4.3.0",
    "node-sql-parser": "^5.4.0",
    "typescript": "^5.9.0",
    "vscode-languageserver": "^10.0.0"
  }
}
