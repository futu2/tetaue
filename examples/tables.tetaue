# Shared table definitions — import this file to get `users` and `orders`.

users = table "users" {
    id = int,
    name = string,
    age = int,
    active = bool,
}

orders = table "orders" {
    id = int,
    user_id = int,
    total = float,
    status = string,
}
